Navigate inside the project download all of the files in same directory and open in youy any local compiler then run:

pip install -r requirements.txt
python app.py 

you will get final results.csv as output 

